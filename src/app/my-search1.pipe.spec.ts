import { MySearch1Pipe } from './my-search1.pipe';

describe('MySearch1Pipe', () => {
  it('create an instance', () => {
    const pipe = new MySearch1Pipe();
    expect(pipe).toBeTruthy();
  });
});
